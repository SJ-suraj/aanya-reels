Originals from the TopView board (full resolution, ungraded), kept so old sets can be re-rendered. A `set` job whose
`urls` point at these files (raw.githubusercontent.com/hbk9sj/travel-reels/main/raw/<name>/0N.png) goes through the
normal grade → pages → Reel path. Zhangjiajie's second photo is from a different outfit; use 01 and 03 only.
